# 更新日志

本文件记录对玩家可见的行为变化。行号级的取证过程在 `docs/上游缺陷诊断.md`。

## 0.7.4

**一次多路对抗式审计的结果。** 把 32 条补丁的前提全部拿回安装的
`crucible-compiled.mjs` / `ember.mjs` / packs 重新推导了一遍 —— 不看本模块自己的注释和文档
（那正是被审计对象），行号一律重新定位。判「有问题」的还要再经三个独立证伪者，
且证伪者被要求**默认倾向推翻**（假警报会让人去改一条本来正确的补丁，比漏报更糟）。

38 条裁定：**35 条前提成立**，1 条前提有误，2 条证据不足。

### ⚠ I5 从来没有执行过一次

判据写的是 `!cardData.targetLabel`，前提是「非 GM 时上游根本没给 targetLabel 赋值」。
**那是错的。** `AttackRoll#_prepareChatRenderContext`（`:3312`）调 `super` 时，
基类 `StandardCheck#prepareDiceResultContext`（`:2202`）已经给**所有人**赋过了：

```js
:2211  const dcLabel = game.user.isGM ? (Number.isNumeric(dc) ? String(dc) : "??") : "";
:2212  targetLabel = dcLabel ? `${label} ${dcLabel}` : label;     // 非 GM → label
:2210  const label = skill?.label ?? defenseType;                 // 攻击骰查不到技能 → "DC"
```

非 GM 拿到的是字符串 **"DC"**（truthy），所以判据恒假。我原先的说法
「`:3328` 那一行只给 GM 赋值」本身没错 —— 但**漏看了基类已经赋过值**。

于是：玩家一直看到「DC」，而设置面板宣称这条已经解决。**比不修更糟**，因为它掩盖了真实状态。

新判据：只在**非 GM** 且 `targetLabel` **还没包含防御名**时替换。不看空不空 ——
要替换的恰恰是那个非空占位符。「还没包含」这一条让上游哪天自己修好后本补丁自动空转。

> **桩件是共犯。** `AttackRollStub` 只复刻了 `:3328` 那半段，没复刻基类赋值，
> 于是 5 条 I5 断言长期对着虚构行为变绿。**桩件只复刻「我以为的那一段」，
> 等于把自己的错误理解写成测试基准。** 本版把整条调用链复刻了出来。

### P3 拆成两条：可证的缺陷 vs 内容判断

P3 一直混着两件性质完全不同的事：

| | 依据 | 性质 |
|---|---|---|
| **P3** `crucible.summons` 里 9 条 `Rune: X` 的 `actions` 是空数组，而 `crucible.talent` 里**同名正版**有 | 同一条目两份数据自相矛盾 | **可证的缺陷** |
| **P3′** ember 四血统有 `system.rune` 却没戏法 | 无 | **内容判断** |

crucible 全文 **`cantrip` 零命中**，`RUNES` 配置（`:5788` 起）也没有戏法字段 ——
**系统从未承诺「有符文就有戏法」**，而那四个血统各自带着自己的招牌动作。
给它们补戏法是内容取舍，不是修 bug。这违反了本模块自己定的排除规则
（「缺的都是设计数据，补丁猜不出来」）。

现在拆成 `patchRuneCantrips`（P3，可证缺陷）与 `patchLineageCantrips`（P3′，内容判断，
提示语里写明）。判据是「这个天赋自己有没有动作」——没有 = 坏掉的快照，有 = 另一种东西。
**两条都默认开启，行为与升级前一致**，只是现在能分开关掉了。

### ⚠ 订正：`expiry` 取 `turnEnd` 的依据说错了

N10 的注释写着「依据是 crucible 自己的转换惯例 —— `SYSTEM.EFFECTS.staggered`(`:5740`)」。
**没有这个惯例。** crucible 自家两个 turns→rounds 生成器**两个值都用**：
`staggered`(`:5740`) 是 `turnStart`，`stunned`(`:5757`) 是 `turnEnd`。
而且被引用的那一条恰好指向与本模块选择**相反**的值。

真正的依据是上游一次迁移 commit，**本机只有编译产物、没有 git 历史，无法复核**。
注释已按实情改写。影响面：选错只让效果多撑或少撑约一个回合；
主前提（turns 会被拒绝创建）是硬事实，不受影响。

### 审计确证的（摘要）

N10 的「38 个动作、crucible 自己占 7 个」与 N12 的「22 条物品自带效果」，
审计方**独立深扫两套 packs 复现了同样的数字**。N12 那条 `transfer:true` 的实例
也定位到了：Mythspire Guardian → Nearing Death(talent) → **Wasting Poison** 效果。
（本模块此前把效果名写成了「濒临死亡」——那是物品名，效果名是「消损之毒」，措辞已订正。）

### 测试

307 → **317 条断言**；变异测试 47 → **53 处，53/53 全部被抓住**。
新增的盯着：I5 的死判据（复现「空才补」必须变红）、GM 那份永不许改写、
P3/P3′ 的判别与四种开关组合。

---

## 0.7.3

补丁逻辑一行未动。修 `diagnose()` 的一处漏报 —— 是用户贴自检输出对数时发现的。

### 修复：`diagnose()` 少报了一条通用补丁

`out.patches.universal` 一栏是**写死**的：

```js
out.patches.universal = UNIVERSAL_PATCHES.length ? ["turnsDuration"] : [];
```

写它的时候通用补丁只有 N10 一条。0.6.1 加了 I7（投掷武器下拉框）之后，
`UNIVERSAL_DEFS` 变成两条，而自检**始终只报一条** ——
I7 一直在正常工作，自检却说它不存在。

现在从 `UNIVERSAL_DEFS` 反查 `label`，不再写死。每条通用补丁必须带 `label`，有断言盯着。

这是本轮第三个同一形状的毛病：**写死的报告行，在新增条目后悄悄腐烂**
（前两个是 0.7.2 修的 `flankingToggleWrapped` 只有 true/false、
以及面板注册状态无从查看）。三处都不影响补丁本身，但都会让排障走错方向。

### 顺带核对（这次的自检输出全部对得上）

| 字段 | 报告值 | 与代码核对 |
|---|---|---|
| `prototypes` | 11 | `PROTOTYPE_PATCHES.push` 11 处 ✅ |
| `hookOverrides` | 1 | `HOOK_OVERRIDES.push` 1 处 ✅ |
| `cantripsCached` | 7 | `RUNE_CANTRIPS` 7 条 ✅ |
| `affixTrainingFixed` | 12 | 12 个符文 Spellcraft 词缀 ✅ |
| `settingsRegistered` | 31 | `bool()` 31 处 ✅ |
| `universal` | ~~1~~ → 2 | `UNIVERSAL_DEFS` 2 条 ❌ **本版修** |

### 测试

303 → **307 条断言**；变异测试 45 → **47 处，47/47 全部被抓住**。

---

## 0.7.2

补丁逻辑一行未动。修一个**从没生效过**的补丁，加固一处排障盲区，重写全部设置提示。

### 修复：I6 首次进世界根本不生效

`installFlankingToggleFix()` 此前**只在 `ready` 里调**，而场景控件的首次渲染在它之前：

| Foundry | 发生什么 |
|---|---|
| `foundry.mjs:205819` | `Hooks.callAll("setup")` —— 我们没在这里装 |
| `:205843` → `:206127` | `initializeUI()` → `ui.controls.render()`，crucible 的顶层钩子创建 `debugFlanking` |
| ready | 我们才注册 `getSceneControlButtons` —— **晚了一步** |

后果：进世界后 I6 是关的，必须切一次控件图层、控件重渲染才补上。
这条是用户把 `diagnose()` 全文贴出来才发现的（`flankingToggleWrapped: false`）。

两道保险：`setup` 阶段就注册钩子；安装时若控件**已经**渲染过，就地把现成那个工具包住
（能立刻生效 —— `#onChangeTool`（`:146118`）是**点击时**才查 `tools[...]`，不是渲染时绑定的）。

### 修复：闸门退让不再静默

上游改写 `debugFlanking` 实现时，I6 原先**一声不吭**地 return。现在打一条警告（只打一次）。
`diagnose()` 也把 `flankingToggleWrapped` 的 `true/false` 换成四种可区分的状态 ——
「上游已改写实现，自动退让」和「安装晚于控件首次渲染」是完全不同的两件事，
以前都显示成 `false`。

### 新增：缓存旧脚本的检测

脚本里写死 `SCRIPT_VERSION`，与 `module.json` 的 `version` 比对。不一致就红字报错 + 常驻通知。

起因：用户报「设置只有十几条、看不到控制面板」，模块列表显示 0.7.0，
实际浏览器执行的是 **0.2.0 的缓存脚本**（Foundry 给模块 ESM 的 URL 不带版本参数，
清单是服务端读的、永远新）。三个信号才定位到：`diagnose()` 少了 `universal` 键、
`active` 恰好 11 条、行号 908。现在版本一对不上就直接喊出来。
`diagnose()` 也补了 `scriptVersion` / `staleScript` 两个字段。

### 设置提示全部重写

**Foundry 用 `innerText` 渲染提示**（`foundry.mjs:41427`）—— 是纯文本。
此前提示里的 `<strong>` `<code>` `<br>` 全部原样显示成字符，比不写更糟。

31 条提示：**HTML 清零**，平均长度 **150+ 字 → 49 字**，全部 70 字以内。
只保留「不开会怎样、开了会怎样」，以及会影响判断的前提
（依赖关系、playtest 合集来源、数值非上游权威、不显示 DC 数字之类的反向承诺）。
根因、行号、上游 commit 全部移出 —— 那些在 `docs/补丁详解.md` 里。

### 测试

280 → **303 条断言**；变异测试 36 → **45 处，45/45 全部被抓住**。
新增的盯着两处此前完全没有覆盖的地方：I6 的安装时机与四个退让分支、
版本一致性检测。其中变异抓出两条假绿：「I6 退让时打警告」和
「`diagnose` 的 staleScript」当时都没有断言约束。

---

## 0.7.1

补丁数不变（32），补丁逻辑一行未动。本版加固 **0.7.0 自己带进来的一个结构缺陷**，外加全仓中文术语对齐。

### 加固：控制面板注册失败会把整个 init 带走

0.7.0 把控制面板的 `registerMenu` 夹在 `bool()` 与 `redirectResource` 之间，
且 `getToolboxClass()` 是**裸调**的。它一抛，后面的 `registerMenu`、`redirectResource`
以及 `applyToggles()` **全部不执行** —— 而 GM 看到的症状只是「设置里少了一截、面板不见了」，
完全看不出是 init 半途死了。一个便利设施不该有能力带走主流程。

> ⚠ **这是按代码推出来的结构缺陷，不是已确认的现场根因。**
> 有用户报「看不到控制面板」，但当时手上没有 VPS 的控制台输出，
> 无法断定是这条塌方、还是 `ApplicationV2` 在 init 阶段没就位（两者症状不同：
> 前者连 P4 的下拉框一起消失，后者只少面板）。本版把**两条路都堵上**，
> 并且让失败本身变得看得见 —— 下次再遇到，控制台会直接说清是哪一种。

- 面板注册**移到 init 最末尾**，整段包 `try`：塌了也带不走任何东西。
- 注册函数改成**幂等**的，并在 `init` / `setup` / `ready` **各重试一次** ——
  `ApplicationV2` 在 init 阶段是否就位取决于 Foundry 版本与加载顺序，拿不到就等下一个阶段。
  菜单是在 `SettingsConfig` 渲染时才读的，ready 阶段补注册照样显示得出来。
- 建类失败 / 注册失败都会打一条 `console.error` 加一条界面警告，不再静默。

### 新增：ready 版本横幅

模块就绪时在控制台打一行：模块版本 / 已注册开关数 / 控制面板是否注册上 / 系统与 Ember 版本。
「设置只剩十几条」这类现象，第一个要回答的问题永远是「在跑哪一版、init 有没有跑到底」，
此前没有任何地方能一眼看出来。面板没注册上时额外再警告一句。

（模块目录常是指向源码的目录联接，**磁盘最新不代表浏览器加载的是最新**，所以这一行值得留着。）

### `diagnose()` 补上版本三件套

输出最前面新增 `version` / `system` / `ember` / `settingsRegistered` / `panelRegistered` 五个字段 ——
报 bug 时贴的就是这段。

### 中文术语对齐

全仓（代码注释、设置面板文案、README / CHANGELOG / HANDOFF / docs / probes）按一套定名统一：
`rank` 一律「阶位」（「等级」只留给 Level）、`action point`「动作点」、`boon`「恩惠骰」、
血统名一律「中文 + 英文」（威伦 Wirrun / 费伊杰 Fej / 弗尔金哈尔 Vrjnhar …），
动作与天赋名改用权威译名（副手攻击 / 猝然撕咬 / 湮解印记 / 曙光信标 / 惑乱凝视 / 穿越翻滚 …）。
`module.json` 的模块简介（模块管理界面直接显示的那段）也一并对齐。
**运行时字符串（如 `statusText: "Mark of Unmaking"`）按定义不动。**

### 测试

257 → **280 条断言**；变异测试 28 → **36 处，36/36 全部被抓住**。
新增的都盯着上面那条塌方点：面板类建不起来时 init 必须继续跑完、
注册函数必须幂等且可重试、`diagnose()` 的五个新字段必须都在。

---

## 0.7.0

补丁数不变（32）。本版是**易用性**：加了控制面板，README 改成看得完的样子。

### 新增：控制面板

**配置与设置 → 模块设置 → Ember / Crucible 临时修补 → 打开控制面板**

此前想知道「这条补丁到底生效没有」只能去控制台敲 `diagnose()` 读一坨 JSON，
而 32 个开关在设置面板里是平铺的一长条，看不出哪条正因为上游已修而自动让路。面板一处解决：

- **每条补丁三种状态**：生效中 / 已关闭 / **上游已修**（开关还开着，
  但检测到上游版本已经修好了这条，于是自动让路 —— 这是正常的，此前完全看不出来）。
- **按钮与控制台命令一一对应**，命令原文就写在按钮旁边。
  会用面板的不必学命令，要写宏 / 报 bug 的可以直接抄走。两边跑的是同一个函数。
- **复制诊断报告**：把自检结果连同 Foundry / 系统 / Ember / 模块四个版本号一起复制到剪贴板。
- **全部开启 / 全部关闭**：想确认「这现象是不是本模块引起的」，一键全关即可回到上游原样，
  不必停用模块再刷新页面。
- 每条开关就地切换，改完立刻能看到状态变化。

控制台侧同时加了 `emberCrucibleTempFix.help()`（打印命令表）
与 `emberCrucibleTempFix.openPanel()`（直接开面板）。

面板的开关清单和设置面板读的是**同一个目录**（注册时顺手记下的），
不会出现两边条数不一致；有断言盯着这一点。

### README 精简

26.7 KB → 8.5 KB。README 现在只回答三件事：**修了什么、控制面板怎么用、目前什么状态**。
每条补丁的根因与做法整段搬进新的 [`docs/补丁详解.md`](docs/补丁详解.md)（内容一个字没删）。

症状表按设置面板的五个分组重排，与面板、与设置面板顺序一致。
「目前状态」明确写清：32 条里**只有 N10 得到过真实验证**，而且被证实的只是「缺陷存在」，
「补丁修好了它」仍待验证 —— 两件事分开记。

### 测试

232 → **257 条断言**；变异测试 22 → **28 处，28/28 全部被抓住**。
新增的断言盯着面板的静默失败：漏渲染一个开关、漏渲染一条命令、
目录与实际注册的开关对不上、组号写错导致整组不显示、批量开关只改一半、
输出区不转义（诊断里含角色名，可能带尖括号）—— 这些都不会报错，只会安静地少东西。

---

## 0.6.1

### 新增 I7 —— 投掷武器的下拉框列出扔不出去的武器（上游 issue #1288）

这条 0.5.0 时判为「需要两处协同修改，留待有把握时一起做」。这次读明白了：
**根因是一处缺失，而且补上它顺带解决了第二个症状。**

crucible 的每个「需求标签」都在自己的 `prepare()` 里把用不了的武器标成不可选 ——
`melee`（`:4148`）排除远程武器、`ranged`（`:4177`）排除近战武器、`natural`（`:4279`）同一形状。
而 `thrown` 的 `prepare()`（`:4250`）**只设了 range，一个字都没过滤**，
尽管它的 `canUse()` 与 `preActivate()` 都对 `!canThrow` 抛错。
`_prepareWeaponChoices()`（`:20134`）把每一条都标 `viable: true` 出厂，
注释明说「Requirement tags further restrict viability」—— 就差 `thrown` 没尽这个责任。

上游 issue 提到的第二个症状（**选了非法的那个之后动作从此点不动，要重启会话才恢复**）
也随之解决：`:4036` 的 `locked` 只在 `getValidWeaponChoices()`（`:20167`，只留 viable）里找，
非法 id 从可选列表消失后就找不到了 ⇒ `locked` 为 null ⇒ 落回正常挑选。
**既进不去那个状态，已经进去了的下一次准备也会自动脱困。**

> ⚠ 「要重启会话才恢复」我**没有复现过**。上面只说明了本补丁为什么让 `locked` 不再卡住；
> 若卡死另有来源，本补丁不保证解决那一半。

做成**通用补丁按标签判**而不是按 action id —— `thrown` 是标签，
`throwWeapon` 不是唯一带它的动作（装备包里的 `net` 也带）。

### 测试

222 → **232 条断言**；变异测试 17 → **22 处，22/22 全部被抓住**。

---

## 0.6.0

补丁 30 → **31**。本版没有新扫一批缺陷，而是把**已有的每一条都拿去对账**，
结果订正了三处硬伤：一条补丁覆盖不全、一个影响面数字漏数一半、版本闸门有十条补丁根本管不到。

### 新增 N12 —— N10 漏掉的另一半

N10 在 `preActivate` 里改动作产出的效果时长，管不到**物品文档自己带的**效果。
后者的创建路径（`transfer:true` 的持有即生效、GM 手动拖、宏里 `createEmbeddedDocuments`）
一条都不经过动作。全包深扫在 `items[].effects[]` 上找到 **22 个**同样坏掉的效果，
最扎眼的是神话尖塔守护者 Mythspire Guardian 的「濒临死亡 Nearing Death」—— `transfer:true`，
**这个天赋一被持有就该生效，实际上永远建不出来。**

只能包 `CrucibleActiveEffect#_preCreate`。挂 `preCreateActiveEffect` 钩子那条路是**死的**：
核心的创建流程（`foundry.mjs:80806-80808`）是

```js
let documentAllowed = await doc._preCreate(...) ?? true;
documentAllowed &&= (noHook || Hooks.call(`preCreate${type}`, ...));
```

`_preCreate` 先跑、crucible 的拒绝就在它里面，而 `&&=` 在左边已经是 `false` 时
**根本不会调用**右边的钩子。与 N10 共用 `patchTurnsDuration` 开关。

### ⚠ 订正：N10 的影响面是 38 个动作，不是 19

早先写的「19 个动作 / 20 条效果，冒险包里还有重复副本」是**漏数**，漏了两类：

- 已经迁成 `units:"turns"` 新形的（`shieldBash`、`steamVent`）——当时只统计了未迁移的 `{turns:N}`；
- 冒险 / 预生成包里 **actor 内嵌物品**上的那些 —— 它们不是「重复副本」，
  而是玩家真会拖上桌用的独立实例。

按不同动作 id 去重实为 **38 个**，其中 **crucible 自己的内容占 7 个**
（`devourThoughts`、`mindFlay`、`eldritchEmanation`、`ferociousHowl`、`pestilentLash`、
`shieldBash`、`steamVent`）—— 也就是说这不只是「ember 数据配 crucible 校验」的接缝问题，
**crucible 单机也踩**。另有 ember 的一批消耗品（炼金手雷 / 霜滴小瓶 / 电荷安瓿 / 三枚宇宙宝石）。

### ⚠ 订正：版本上限此前对十条原型补丁完全不生效

`fixedIn` / `fixedInEmber` 只作用于 `PATCH_DEFS` 与 `UNIVERSAL_DEFS`，
而 **B 系列（上游一发布就该退休的那五条回搬）恰恰全是 `PROTOTYPE_PATCHES`** ——
写了也是死字。现在改成中央表 `VERSION_CEILINGS`，三类补丁走同一条判定，
B1–B5 填 `0.10.2`（0.10.2 至今未发，所以现在全部空转）。

### 其他订正

- **X1 的覆盖面是扫出来的了。** 把两侧 packs 全部 409 条动作过了一遍，
  先按 `propagate` 求闭包（`natural`→`melee`→`strike` 这种链会补上掷骰实现，
  不求闭包会误报 13 条），结果只有三条真的缺实现：X1 这两个，
  加上 ember 的 `waterAversion`。第三条**故意不修** —— 它自己的描述里写着
  「ALPHA ONE: This action needs further balancing」，是作者挂着的半成品。
  同时说清楚 X1 **补的是实现不是数值**：防御、伤害类型、属性加值、−6 减值
  全部读自动作自己的标签，本模块不发明任何数字。
- **`docs/上游缺陷诊断.md` §7 撤回一条。** 那里把 `swallow` 的 17 字符 `_id` 归为
  「只上报、不补」，理由写的是「因 scope 未设而潜伏」——**臆断**。
  `_id` 的合法性与 scope 无关，它和 N1 是同一条链。它现在是 C1，已修。
- **`patchDamageTypes` 的提示补上前提**：`devourThoughts` 来自 playtest（试玩）合集，
  没导入过它的世界这一条自然不触发。
- **`HOOK_OVERRIDES` 现在认 `setting` 字段** —— 此前 N1 的天赋侧覆盖是唯一
  「关不掉」的补丁，与 README 里「每一项都能单独关掉」的说法不符。
- **C1 关掉开关后不再还原坏 ID** —— 还原会让已经吞下去的目标永远吐不出来。

### 设置面板重排

30 个平铺开关按「① 影响最大 / ② 动作放不出去 / ③ 结算错了 / ④ 回搬自上游 / ⑤ 显示与界面」
五组重排，名称带组号前缀。**设置键一个都没改**，升级后已有的开关状态照旧。

### 遍历器的 BLOCKER 已修复

`probes/console_action_sweep.js` 此前被 `iUnderstandTheBlocker` 闸门锁着，
因为它跳过 `_canUse()` 直接调 preActivate，会让 `delay` 等四个动作弹出几百个模态对话框，
**而写盘熔断在此期间一直遮着 `DatabaseBackend` —— 世界会写不进任何东西。** 三处修复：

1. `MODAL_PREACTIVATE_IDS` 跳过表，走「未覆盖」分支而**不是** `return null`
   （后者会被记成「S2 跑通了」，拿没跑过 preActivate 的形态去判定 = 假绿）。
2. 遮 `DialogV2.wait/prompt/confirm/input`，漏网的弹框立刻抛错并记账。
   还原走 `getOwnPropertyDescriptor` + `defineProperty` —— 这四个是类自身的 own static 属性，
   用 `delete` 还原会把真实实现永久删掉。
3. 三个熔断/挂起动作全部移进 `try`，restore 先初始化成空函数。

另外两处：新增 **D-D3** 断言覆盖 N12；效果文档扫描此前只收 world actor 与世界物品，
**漏掉了 actor 内嵌物品那一层** —— 而 N12 的目标恰恰全在那里。
B-W1（覆盖 I1）加了可判性检查：宿主自带天生武器时它的判据永远不成立，
现在会明说「本次跑不出 I1」，而不是让报告显示成「未命中」。

### 测试

201 → **222 条断言**。变异测试脚本这次**进仓库了**（`tests/mutate.mjs`，此前每次现写）：
17 处变异，**17/17 全部被抓住**，含一条此前的假绿 ——
`settingOn` 的「读不到设置就保守生效」这条失败方向从来没被断言过，现在补上了。

### 第一条被现实证实的诊断

用户实测「强健体力」用了没任何效果、1 专注 + 1 点英雄气概白扣 —— 与 N10 的诊断逐字吻合。
这是 31 条里**唯一**一条得到真实验证的。注意「缺陷存在」被证实了、
「补丁修好了它」还没有，两件事分开记。

## 0.5.0

补丁 27 → **30**。本版收尾显示与交互层三条。

- **I5（上游 issue #1402）：玩家端的攻击卡只写「DC」。**
  `AttackRoll#_prepareChatRenderContext`（`:3311`）其实**给所有人**都算好了 `defenseType`，
  但模板渲染的是 `{{targetLabel}}`，而那一行**只给 GM 赋值**。
  防御类型本来就是公开信息（条目描述里白纸黑字写着「攻击其反射」），该藏的只有 DC 数值。
  开启后玩家端补上**类型**，**仍然不显示 DC 数字** —— 有专门的反向断言盯着这一点。
- **I6（上游 issue #1311）：「可视化夹击」的叠层关不掉。**
  工具的 `onChange`（`:47965`）只遍历 `canvas.tokens.controlled`，
  换选之后上一个 token 的图形就成了**孤儿**：钉死在画布上、再点开关也清不掉，只能刷新页面。
  开启后关闭时清理**全部** token。
- **B5（上游 `ac1b5cfc`）：侧栏「当前装备」最多只列得出 1 件天生武器。**
  循环上界写成 `i < 3 - featuredEquipment.length`，而每 push 一件上界就缩一格。
  纯显示层 —— 动作列表里那些打击照常在，命中与伤害不受影响。

### 仍然不做

- **#1288（投掷武器下拉框列出徒手/天生武器）** —— 需要两处协同修改，
  规格明写「只做前一半会比不修更糟」。留待有把握时一起做。
  > **0.6.1 已做**（I7）。当时判断错了：不是两处协同，是**一处缺失** ——
  > `thrown` 标签漏了别的需求标签都做的 `viable` 过滤。补上它之后，
  > 「卡死」那一半也随之解决，因为锁定的武器只在 valid 集合里找。
- **C6**（蒸汽喷口半径）、**E3**（混沌折射恩惠骰）—— 理由见 0.4.2 与更早版本。

### 测试

184 → **192 条断言**；变异测试 48 → **52 处，52/52 全部被抓住**。

## 0.4.3

补丁数不变（27 条），本版全是**闸门口径**的收口 —— 来自与上游对账里 14 条有行动项的条目。
其中大部分（D 系列、N11、C1、N10 的源码闸门、P1 的 `__guard`、B1–B4）在前几版已经做掉，
这一版补上剩下的三条。

### 新增：Ember 版本轴

版本上限此前只比 `game.system.version`（crucible）。但**ember 侧的数据缺陷该按 Ember 版本封顶** ——
上游修 Ember 的数据不等于 crucible 发版。

依据是上游自己的做法：crucible 内建的 `EMBER_PATCHES`（`:45771`）由 `applyEmberPatches()`（`:45782`）
按 `if (isNewerVersion(ember.version, emberVersion)) continue;` 闸门。
现在 `fixedInEmber` 与它同源。两条轴的失败方向都是**保守生效**。

### 新增：N11 的退休闸门

上游一旦给 `CrucibleActor` 补上 `training` getter（issue #1423 的正解），
ember 那句 `this.training[runeId]` 自己就能跑通 —— 本条即自动停用，不再去改那 12 个词缀钩子。

### 顺带核实：crucible 自带的 Ember 兼容层不会与本模块冲突

`EMBER_PATCHES` 只登记了 `"0.5.1"`，而闸门是「ember 比它新就跳过」——
**装 Ember 0.6.0 时那一整层是关掉的**，不会与本模块争同一批钩子。已写进 README 兼容性说明。

### 复核后维持原判的两条

- **N9 的闸门不改用 `TALENT_ID_MIGRATIONS`。** 对账建议用那张表判定「陈旧快照」，
  但它只覆盖 4 个废弃小写 id，而现有判据 `if (item.system.training?.type) continue;`
  直接看症状本身，还能盖住 ember 那四个 id 正确但 training 为空的血统。**收窄了不划算。**
- **N3 的 `expiry` 维持 `turnStart`。** 对账里有一条单路证据主张改 `turnEnd`，
  与 crucible 自家 `SYSTEM.EFFECTS.staggered` 生成器的产物冲突 —— 以生成器为准。

### 测试

179 → **184 条断言**；变异测试 46 → **48 处，48/48 全部被抓住**。

## 0.4.2

### E2：两个血统的招牌能力从来没触发过

效果 ID 由 `getEffectId(actionId, {suffix})`（`:5358`）确定性生成
（`generateId` `:48066` 是 `slice(15).padEnd(15,"0") + suffix`），而 ember 两处查的是**猜出来的串**：

| 动作 | 系统实际生成 | ember 查的 |
|---|---|---|
| `implacableHunter` | `implacableHunte0` | `implacableHunter` |
| `formidableStamina` | `formidableStami0` | `formidableStamin` |

后果：威伦 Wirrun 血统的角色花 1 专注 + 1 点英雄气概标记猎物后，**朝猎物的攻击一次 +2 恩惠骰都不会出现**；
弗尔金哈尔 Vrjnhar 血统的「强健体力」（动作点见底时退还 1 点）**从头到尾一次都不触发**。

修法特意选了**轻的那一边**：ember 查的那两个串本身就是**合法的 16 位 id**，
而这两个动作又**各只有一条效果**（无歧义）。所以不去替换 ember 的钩子（要照抄几十行），
而是把**写入端**的 `_id` 设成它要找的那个 —— 两边自然对上，代码量十分之一。
闸门认 ember 源码里那个坏查询串：它改对了我们就退让，让系统照常生成真实 id。

### 明确不做：E3（混沌折射的恩惠骰）

机制已查清：ember 把 boons/banes 写在 effect 的 `flags` 上（`ember.mjs:125482`），
而 crucible 重建效果时（`:19806`）只解构 `name/scope/result/statuses/duration/system`
—— **`flags` 被整个丢掉**。聊天卡明写「Boons +3」，下一个动作的骰池里一颗都没有，
效果图标还照样被消耗掉。

不做的理由：要修就得**另找一条能存活的通道**。`effect.system` 有 schema 会剥掉野键，
actor flags 那条的时序我无法离线验证。**发明一条验证不了的持久化通道，风险大于收益**
—— 与 C6（蒸汽喷口半径）同一个判断。

### 测试

174 → **179 条断言**；变异测试 43 → **46 处，46/46 全部被抓住**。

## 0.4.1

两条「数值悄悄算错」——玩家看不出来，但影响每一次结算。

- **I4（上游 issue #1404）：位移动作重复准备，标签加成被累加。**
  `_configureUsage()` 的注释白纸黑字写着
  「so that repeated prepare() calls **do not accumulate costs**」，但它**只重置了 cost**；
  `usage.bonuses` 的归零只发生在 `_prepareData()` 里，而 `prepare()` 不调它。
  于是遇上 `empowered` 这种 `damageBonus += 6` 的累加写法，位移规划流程里的第二次
  `prepare()` 就把加成叠了一遍 —— 飞踢伤害**比条目描述多 6 点**；
  路径被判非法需要重新规划时会再叠一次，变成**多 18 点**。
  修法是在委托原实现之前把六个加成字段恢复成上游自己那份「原始态」，逐字对齐它的归零块。
- **E1：「稳定守护」把酸性抗性算成 `NaN`。**
  它的 change 写的是 `system.resistances.acid`，**少了 `.bonus`** ——
  而那一层按 schema 是个 `SchemaField`，不是数字。
  此后每一次酸性伤害结算都带着 NaN 传播（伤害数字变 NaN、聊天卡伤害栏是空的）。
  同 pack 的其它 13 篇带 changes 的文档全都规规矩矩写了 `.bonus`，**只有它一条落错层**。

### 测试

164 → **174 条断言**；变异测试 39 → **43 处，43/43 全部被抓住**。

## 0.4.0

补丁 20 → **24**。首次覆盖**上游还没修的开放 issue**。

### 新增 I 系列：上游开放 issue（还没修的）

- **I1（issue #1403）：「狂野打击」能白刷动作点。**
  `natural` 标签的判据是 `strikes.every(...)`，而 `every` 对**空数组返回 true** ——
  没有天生武器的角色照样通过校验。动作显示可用、生成聊天卡，
  但一次骰都不掷、一点伤害都不出，**反而把动作点退还回来**。
- **I2（issue #1412）：手工添加的知识不生效。**
  `hasKnowledge` 只读 `details.background.knowledge`（背景条目自己那份快照），
  而角色实际的知识聚合在 `details.knowledge`。GM 手工加的知识用「评估力量」「洞悉弱点」时，
  **本该拿到的 +2 恩惠骰不会出现**。
- **I3（issue #1406）：私人传记泄漏。** 角色卡**无条件**把 private 三件套塞进渲染上下文，
  没有任何权限判断。GM 把 NPC 设成 limited 让玩家查基本信息，玩家切到生平页
  **就能原文读到 GM 私记**。提示语写着「仅拥有者可见」，实现没兑现。

### 新增 B4：回搬（此前因许可证跳过）

**B4（上游 `798a8638`）：多技能团队检定里换了技能，掷的还是默认那个。**
`rollSkill` 把 `check` 声明成 `const`，对话框的返回值只用来判断有没有取消，**本身被丢掉了**。

这是唯一一条**整体替换**（其余回搬全是包装）—— 修改点在函数中段，包装够不着那个局部量。
闸门认的是**有 bug 的原句** `const check = this.getSkillCheck`：上游的修法正是把它改成
`let check`，所以上游一发布，闸门立刻失配、本条自动退休。

### 测试

148 → **164 条断言**；变异测试 35 → **39 处，39/39 全部被抓住**。

## 0.3.1

### 新增：从上游开发版**回搬**修复

方向与之前所有补丁相反 —— 上游 `master` 已经修好、但**还没发布**的 bug（0.10.2 至今未发），
我们先搬给还在 0.10.1 的人用。

- **B1/B2（上游 `bea623d8`，修 #1378 与 #1396）：词缀推导出来的附魔加值完全不生效。**
  根因是**算得太早**：0.10.1 在 `prepareBaseData` 里就把附魔加值算死
  （武器 `:45092`、护甲 `:44225`），而**词缀**要到派生阶段才解析完。
  玩家看到的是：给武器加词缀后物品卡上等级显示正确，但攻击掷骰里**没有附魔加值**；
  手动把等级改成同一档反而就有了。护甲那面是闪避防御不涨。
- **B3（上游 `1659465a`，修 #1379）：角色卡弹成独立窗口后货币显示为 0。**
  货币元素在首次构建时把自己的 `value` 属性删掉了，元素被搬进弹窗重新连接时只能读回 0。
  改动一次货币又会自己好（重渲染会重新写上属性）。

> **⚠ 回搬一律不照抄上游代码。** crucible 的 LICENSE 明写
> 「No permission is granted at this time to modify, publish, distribute, sell, or otherwise use
> the software or its data in any other way.」所以全部用**包装**实现 ——
> 读懂上游 diff 修了什么，然后用自己的代码达到同样效果。
> 需要整段照抄才能修的（`798a8638` 的 `rollSkill`）**不做**，见下。

### 新增机制：包装任意类的原型方法

第四个注入点 `PROTOTYPE_PATCHES`。前三个（`ACTION_PATCHES` / `UNIVERSAL_PATCHES` /
`HOOK_OVERRIDES`）都只能碰动作与钩子注册表；这一个能包**数据模型、自定义元素、角色卡**。
带源码特征串闸门，开关在包装体内部实时读 —— 关掉即刻回到上游原行为，不需要卸载。

### 明确不做的

- **`798a8638`（多技能团队检定里换技能不生效）** —— 修法需要整段替换 `rollSkill`（约 25 行），
  而 crucible 不是开源许可。触发面也窄（只有多技能团队检定一条路径）。**记录不修。**
- **`33bc14ff`** —— 它修的是未发布代码 `6b5551d6` 自己引入的**回归**，
  0.10.1 编译产物里 `ownedRef` 零命中，搬过来是无中生有。
- **`b1d76df0`（攻击结算重构）** —— 叫 "Refactor"，且搬过来会让 Bastion 守御姿态的
  RESIST→BLOCK 从死代码变成生效。是修 bug 还是改平衡没有把握，**不闭眼搬**。

### 测试

136 → **148 条断言**；变异测试 30 → **35 处，35/35 全部被抓住**。

实测抓出一个设计缺陷：**闸门检查会被自己的包装污染** —— 同一个类有多条补丁时，
后一条的 guard 去读的方法可能已经被前一条包过，读到包装体、特征串必然找不到，
于是补丁被自己人挡在门外。现在统一读 `__tempfixOriginal`，并有专门的变异用例锁住。

## 0.3.0

补丁数从 13 增到 **18**，其中 **5 条是 crucible 系统自身的缺陷**（此前只修 ember 侧）。

### 新增：crucible 自身的缺陷

- **C1 「吞下」的效果 ID 有 17 个字符** —— crucible 自家的 N1。硬编码常量
  `"swallowed00000000"`（`:10530`）比合法长度多一位，动作抛异常中止：**什么都不发生、
  连聊天卡都不生成、资源也不扣**，配套的「反吐」也永远找不到要删的效果。
  影响 5 个 adversary（穆塔普 Mootap ×2 / Sarracenias / 两条黑曜藤蔓）。
  > 顺带订正：诊断文档此前写「因 scope 未设而潜伏」——**那是没查数据得出的**。
  > 实测该动作第 0 条效果的 `scope` 就是 3，`find(e => e.scope === ENEMIES)` 必然命中。
- **C4 「穿越翻滚」下的动作「翻滚 `tumble`」目标阵营写成盟友** —— 描述两次点名敌人，而 `target.scope = 2`。
  选中敌人点它会报「阵营不合法」，只有选队友才能用。
  实现上挂 `initialize` 而非 `prepare`：crucible 自己在 `:10958` 定义了 `tumble.prepare`，
  同名会把它顶掉。
- **C5 「曙光信标」是 60 尺 pulse 却把 scope 写成自身** —— 区域取目标那条路径没有空集保护，
  光柱画出来了但零目标零骰子。
- **N11 12 个符文 Spellcraft 词缀不设训练阶位**（对应上游 issue #1423）——
  钩子把训练阶位写到 actor **文档**上，而 `training` 只存在于数据模型上，
  异常被 `callActorHooks` 吞成 console.error。真实症状不是「建卡失败」而是**静默降级**：
  符文知识拿到了、训练阶位没设上，该符文的法术按未受训 −4 结算。

### 新增：描述与数据矛盾

- **X1** 「令人作呕的脓疱」「深渊遗骸」缺任何提供掷骰实现的标签，描述承诺的伤害完全不会发生。
  （已按 P4 的教训逐个 grep 钩子注册表确认这两条在 crucible 与 ember 里都没有代码侧自动化，
  才敢补标签。）
- **D 系列** 三条动作的伤害类型与自己的描述矛盾：`noxiousSpray` 剧毒喷雾结算成电击、
  `selfDestruct` 烈焰爆炸结算成穿刺、`devourThoughts` 灵能伤害落回天生武器的钝击。
  后果是抗性算错。**这一类的判据来自上游自己**——`master` 的 commit `2cfed5dd` 就是
  「把毒喷的伤害类型从电击改成毒，*as mentioned in the description*」。

### 新增机制：版本闸门（第三种闸门）

模块现在三种闸门各管一段：**数据形状检测**（上游改了数据或校验）、
**`__guard` 特征串**（上游重写了我们要顶掉的那段）、**版本上限 `fixedIn`**（上游修好了但运行时看不出来）。

关键是**加上限而不是删除** —— 还装 0.10.1 的人仍然需要这些补丁。
两条回退路径都是**保守生效**：读不到系统版本、或读不到比较 API，都继续打补丁。

### 测试

94 → **136 条断言**；变异测试 19 → **30 处，30/30 全部被抓住**。

变异测试这一轮又抓出两个假绿：X1 的「已有 roll 提供者就不再加」用了 `generic` 做对照，
而 Set 的 `add` 幂等、漏检也不会变化；C1 的「上游修好后不该报警」被警告去重吃掉，
**子用例顺序本身是被测行为的一部分**。两条都不是补丁问题，但不做变异测试会一直假绿。

## 0.2.2

### ⚠ 订正 v0.2.1：N10 的 `expiry` 方向写反了

`turnStart` → **`turnEnd`**。依据是上游自己迁移**同一种数据**时的映射：
commit `48bf4391f7`（PR #695「Migrate ActiveEffect expiry to V14 native schema」）
把自家 `_source` 里旧的 `{turns:N, rounds:null}` 全部迁成
`{value:N, units:"rounds", expiry:"turnEnd"}` —— 实测 **49/49，零例外**。

`turnStart` 是上游给 `{rounds:N}` 那种数据的映射，套到 turns 数据上会让九大血统的变身
**多撑约两个 turn**。

**N3（斥退踢击）保持 `turnStart` 不变** —— 它的数据不是 turns 型，
最近的权威是 crucible 自家的 `SYSTEM.EFFECTS.staggered` 生成器，产出就是 turnStart。

### 新增：P1 的上游退让闸门

P1 是唯一「整体顶掉上游实现却没有闸门」的补丁。今天无害（上游 `canUse` 一字未改），
但上游哪天自己修好，我们会带着旧逻辑继续跑 —— 那不是双重应用，是**静默替换**。

现在 `ACTION_PATCHES` 支持 `__guard`：列出上游实现里必须还能看到的特征串，
看不到就**逐键退让**并警告一次。纯追加的键（上游没有同名钩子）不受约束。

### 订正统计数字

`_source.slot = 0` 的武器实测数，由「85 件 / 61 个 actor / 其中 11 个带 Dual Wield」
订正为 **161 件 / 111 个 actor / 其中 9 个带 Dual Wield**。
范围：`ember.crucible-adventure` 的 265 个 actor；过滤条件为 equipped 且 category 非 natural/unarmed；
含场景 token delta 结果不变。原数字三项全错，是引用了未经复核的结果。

> 顺带核实：受 N10 影响的动作数**仍是 19 个 / 20 处**（全 10 个 ember pack 都扫过，
> 其余 8 个 pack 零命中）。上游对账里「32 个 / 92 处」那个说法未能复现。

### 测试

94 → **97 条断言**；变异测试 17 → **19 处，19/19 全部被抓住**。

## 0.2.1

### 新增 N10 —— 目前影响面最大的一条

**19 个动作的效果从来没有落地过**，其中包含**九个血统的招牌变身**
（阿尔提拉 Altyra 泰拉菲克变形、科拉克 Cor'ak 结晶化创伤、费伊杰 Fej 极限代谢、
赫尔格伦 Hulg'run 活石、基瓦尔 Kivahr 规整节律、荆芽灵 Thornling 刺棘树皮、
弗尔金哈尔 Vrjnhar 强健体力、威伦 Wirrun 无情猎手、泽夫 Zeph 的三种面容）。

玩家看到的是：**聊天卡白纸黑字写着「获得 XXX · 持续 ∞」，角色身上一个图标都没有。**

根因是数据写的是 v12 时代的 `{turns: N}`，核心迁移成 `{value: N, units: "turns"}`（迁移本身成功），
随后被 `CrucibleActiveEffect._preCreate` 的 `["months","turns"].includes(units) → return false` 当场拒绝。
开启后把单位换成 `rounds`、数值不变、补上 `expiry`，与 crucible 自己对 staggered 的转换惯例一致。

> 这是**解释**不是还原：上游没有 turns 这个单位，原作者想要多久无从考证。所以有单独开关。

这是本模块第一条**通用补丁**（对每个动作都跑）。它作为额外的一格注入、**不与动作自带的钩子合并**，
所以顶不掉任何上游实现 —— 这一点有专门的反向断言把关。

### ⚠ 订正：v0.2.0 的 N2 补丁是空转的

`sentinelShielding` / `tyraphicTransformation` 的效果**同时**踩了 N10 ——
`units:"turns"` 使它们根本不会被创建，往一个不存在的效果里写 `system.changes` 毫无意义。
v0.2.0 的 README 写的症状「图标照常挂上，但加值不生效」也是错的：**图标根本不会出现**。

N2 的实现本身没问题，但**必须与 N10 一起开才有意义**。两者现在都默认开启。
（顺带：`bewilderingGaze` 的 confused 效果也在这 19 个里 —— N5 修好了它的攻击判定，
但它施加的效果同样从来没落地过，v0.2.1 起才真正完整。）

### 测试

- 断言 74 → **94**，新增的 16 条覆盖迁移链、单位改写、以及「通用补丁不能顶掉按 id 的补丁与 ember 的钩子」。
- 变异测试 13 → **17 处，17/17 全部被抓住**，其中包括把通用补丁改成与 hooks 合并（会立刻红 7 条）。

## 0.2.0

### ⚠ 破坏性订正

- **P4「疗愈导流」整条撤回重写。** 0.1.0 的实现建立在一个错误前提上
  （「数据里没有 `actionHooks` 键 ⇒ 这个动作没有自动化」——**系统压根不读那个字段**，
  钩子按 action id 从 `crucible.api.hooks.action[id]` 取，而 ember 在代码里注册了 43 个）。
  旧实现给动作加了 `generic`，实际后果是：对**队友**掷一次针对创伤阈值的攻击骰，
  同时 ember 自己的 `postActivate` 照跑 → 同一次动作记两笔恢复。**开着比关着更糟。**
  新实现只覆盖 `canUse`，只修一件事：把恒为生命值的恢复资源改成那次被抵抗的骰子里的真实资源。
- `redirectResource` 设置项默认值由「生命值」改为「自动推断」（否则新补丁默认什么都不做）。
- 修复自动推断读错字段名（`ev.roll` → `ev.rollIndex`）—— 旧版 auto 档 100% 失效且静默退回生命值。

### 新增补丁

- **N1** `abyssMarkUnmaking`：硬编码的效果 ID 只有 15 个字符（要求恰好 16），
  导致整个动作抛异常中止 —— 不扣资源、不生成聊天卡。动作侧与天赋侧一起修。
- **N2** `sentinelShielding` / `tyraphicTransformation`：`changes` 写在效果顶层而非 `effect.system` 下，
  图标挂上但加值一条不生效。（威吓 +2 恩惠骰那一条系统层面表达不了，仍未生效。）
- **N3** `sentinelKick`：`duration` 有 value 没 units，被系统整段丢弃 →
  踉跄永不消失，中招者每回合永久 −2 动作点。
- **N4** `heartSparkOfEmber`：作用域写成「敌人」，复活友方的分支永远选不中目标。
- **N5** `bewilderingGaze`：缺 `willpower` 标签，精神攻击按护甲结算。
- **N6** `antigravityStone`：纯自身效果写成「单体且不可选自己」，必须拿别人凑数才能用。
- **N7** `darkflameCirclet`：`composed` 标签只对法术动作合法，导致使用时崩在生成聊天卡之前。

### 扩充既有补丁

- **P1** 补上徒手 / 临时武器路径。`UNARMED_DATA` 里根本没有 slot 字段、
  `_prepareWeapons` 的徒手赋值一个字都不设手位、`_getUnarmedWeapon` 产的实例没有 `_id`
  —— 0.1.0 的三级判据在空手时**全部落空**。这是这条缺陷最主的成因，不是边角情况。
- **P3** 改为按 **rune** 查表（原先按血统 item id），一次盖住 ember 四血统与
  `crucible.summons` 里 9 条 0.9.0 旧快照，并补上被丢掉的训练阶位（否则本命符文法术按「未受训 −4」算）。

### 自身修补

- 切换设置后角色卡现在真的会刷新：crucible 的卡全是 ApplicationV2，
  而 0.1.0 只遍历 `ui.windows`（V1），里面一个 crucible 角色卡都没有。
- P3/N9 注入的动作不再传 `parent` —— 传了会让动作配置卡一改字段就弹红条报错。
- `module.json`：删掉空的 `manifest`/`download`（schema 是 `blank:false`，空串会挂黄色警告）；
  `compatibility.minimum` 由 13 改为 14（crucible 0.10.1 自己就要求核心 ≥14.364）。
- 非 crucible 世界现在在 `init` 最前面就 return，不再注册任何设置项。

### 测试

- 断言 20 → **74** 条，补上五处桩件盲区（聊天记录恒空、`prepareData` 不清空动作表、
  设置项 `onChange` 从不触发、标签集接受任意字符串、缺 `hooks` 快照桩）。
  正是这些盲区让 `ev.roll` 这个 100% 失效的 bug 一路绿灯通过。
- 加了变异测试：把 13 处补丁逐个改回坏写法，确认桩件真的会红。**13/13 全部被抓住。**

## 0.1.0

首个版本：P1 副手攻击判据、P2 凯思血统「猝然撕咬」范围、P3 血统所授符文的戏法、P4 疗愈导流（**已知有害，见 0.2.0**）。
